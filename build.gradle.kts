// Top-level build file where configuration options
// common to all modules are declared.

plugins {
    alias(
        libs.plugins.android.application
    ) apply false

    alias(
        libs.plugins.google.services
    ) apply false
}